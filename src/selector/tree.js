/**
 * selector/tree.js implements:
 * - ROM.selector.SelectorTree (constructor)
 * - ROM.selector.SelectorNode (constructor)
 * - ROM.selector.tree (instance of SelectorTree)
 */

window.ROM.selector.SelectorTree = function SelectorTree() {

};

window.ROM.selector.SelectorNode = function SelectorNode() {

};

window.ROM.selector.tree = new ROM.selector.SelectorTree();
